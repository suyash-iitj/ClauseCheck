import React from "react";
import ReactDOM from "react-dom";
import ClauseCheckApp from "./ClauseCheckApp";

ReactDOM.render(
  <React.StrictMode>
    <ClauseCheckApp />
  </React.StrictMode>,
  document.getElementById("root")
);
